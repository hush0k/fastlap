# apps/bloggers/urls.py
"""
URL configuration for the bloggers app.
"""

# Django modules
from django.urls import path

# Project modules
# from apps.bloggers.views import BloggerViewSet

urlpatterns = [
    # path("", BloggerViewSet.as_view({"get": "list"}), name="bloggers"),
    # Add your blogger URLs here when ready
]